
// FieldsOptions is an object with keys {type, editable, sortable, filterable, label, options?}
/**
 * @typedef {Object} FieldsOption
 * @prop {string} key
 * @prop {string} type
 * @prop {boolean} editable
 * @prop {string} label
 * @prop {any=} options
 */

export const Types = {}